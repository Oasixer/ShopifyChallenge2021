type Mutation {
  signUp(
    email: String!
    password: String!
    username: String!
  ): User!
  signIn(email: String!, password: String!): String
  changePassword(password: String!): User!
  saveFile(name: String!, uuid: String!, tags: String!): File!
}
